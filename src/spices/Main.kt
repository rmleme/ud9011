package spices

fun main(args: Array<String>) {
    //val simpleSpice = SimpleSpice()
    //println("name = ${simpleSpice.name}   heat = ${simpleSpice.heat}")

    /*val spices = listOf<Spice>(
        Spice(name = "Pimenta Verde"),
        Spice(name = "Pimenta do Reino", spiceness = "soft"),
        Spice(name = "Jalapeno", spiceness = "spicy")
    )

    val softSpices = spices.filter { it.heat < 5 }

    val salt = makeSalt()*/
}